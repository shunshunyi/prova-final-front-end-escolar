
export interface Student {
    id: string;
    name: string;
    age: number;
    grade: string;
}

export interface Course {
    id: string;
    title: string;
    description: string;
    duration: string;
}
